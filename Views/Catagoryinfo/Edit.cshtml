@model GiftYourbeloved.Models.Catagoryinfo

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Catagoryinfo</legend>

        @Html.HiddenFor(model => model.catagory_ID)

        <div class="editor-label">
            @Html.LabelFor(model => model.Catagory_Name)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Catagory_Name)
            @Html.ValidationMessageFor(model => model.Catagory_Name)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Description)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Description)
            @Html.ValidationMessageFor(model => model.Description)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
